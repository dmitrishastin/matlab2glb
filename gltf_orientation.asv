function data = gltf_orientation(data)

    % RAS to LSA
    
    if ~iscell(data)
        d2c = true;
        data = {data};
    end
    
    cn = 1:size(data{1}, 2);
    pc = [1 3 2 cn(
    
        data = cellfun( @(data) permute(data, [, data, 'un', 0);
    
    data = cellfun( @(data) [-data(:, 1) data(:, 1:end)], data, 'un', 0);

end